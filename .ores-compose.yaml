schema_version: ores.compose.v1
project: evgl-mcp-server.rs
allow_lazy_start: false

services:
  evgl-mcp-server.rs:
    name: evgl-mcp-server.rs
    runtime: host
    build:
      - ["cargo", "build", "--bins"]
    command: ["./target/debug/evgl-mcp-server.rs"]
