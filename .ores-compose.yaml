schema_version: ores.compose.v1
project: evgl-mcp-server
allow_lazy_start: false

services:
  evgl-mcp-server:
    name: evgl-mcp-server
    runtime: host
    build:
      - ["cargo", "build", "--bins"]
    command: ["./target/debug/evgl-mcp-server.rs"]
